extend type Mutation {
  brandLocationFacilitySave(
    facilityInput: BrandLocationFacilityInput!
  ): BrandLocationFacilityPayload!
    @requireAuth
    @requirePermissions(permissions: ["brandlocation:upsert"])
}
